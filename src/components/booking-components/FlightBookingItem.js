import { View, Text } from "react-native";
import React from "react";

const FlightBookingItem = ({ data }) => {
  return (
    <View>
      <Text>FlightBookingItem</Text>
    </View>
  );
};

export default FlightBookingItem;

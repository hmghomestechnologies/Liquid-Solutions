import { View, Text } from "react-native";
import React from "react";

const MenuOrderDetails = ({ orderDetails }) => {
  console.log(orderDetails);
  return (
    <View>
      <Text>MenuOrderDetails</Text>
    </View>
  );
};

export default MenuOrderDetails;
